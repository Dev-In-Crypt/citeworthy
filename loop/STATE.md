# STATE.md — память цикла (на диске, не в контексте)

> Обновляется агентом в конце КАЖДОЙ итерации. Секреты сюда не писать.
> Формат записи: дата/итерация, задача, статус, что дальше, блокеры.

## Текущий статус

- **План пройден.** Осталось только то, что заблокировано человеком: T08 (нет git-remote), T13–T15 (ключи платформ), T54 (Stripe). Всё остальное отмечено.
- Сделано: Phase 0–5 закрыты, кроме перечисленных блокеров. Тестов: 484 юнит/интеграционных + 29 e2e. Пайплайн вынесен в @repo/pipeline; T08 написан, но не отмечен (нет remote).
- Команды проверки: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, `pnpm e2e`, `pnpm db:migrate`, `pnpm db:check`, `pnpm db:seed`.
- e2e поднимает свой сервер на порту 3100 из production-сборки. Если тесты падают мгновенно и «не видят» правок — на 3100 висит старый процесс: `Get-NetTCPConnection -LocalPort 3100 -State Listen` и `Stop-Process`.
- Порты: Postgres `localhost:5433`, Redis `localhost:6380` (нестандартные, чтобы не конфликтовать с локальными сервисами). Перед работой: `docker compose up -d`, `.env` копируется из `.env.example`.

## Блокеры для человека

- ~~БЛОКЕР (2026-08-12): Docker Desktop не поднимался~~ — СНЯТ пользователем 2026-08-12. Симптом на будущее: Процессы запущены, но WSL-дистрибутив `docker-desktop` в состоянии Stopped; `wsl --shutdown` и рестарт приложения не помогли. Скорее всего Docker Desktop показывает окно, требующее действия (обновление, вход, принятие условий). Открыть приложение и посмотреть. Проверка: `docker info` должен отвечать, затем `docker compose up -d && pnpm db:migrate`.

- **БЛОКЕР T54: нет Stripe-аккаунта.** Нужны: аккаунт, три Price ID (по плану), webhook secret. До этого checkout и портал проверить негде — задача пропущена, цикл ушёл в Phase 5.
- **T55: доставка в Sentry не проверена.** Код работает без DSN (ошибки идут в структурный лог), проводка до SDK покрыта тестами с подменой. Чтобы включить прод-канал: задать `SENTRY_DSN` (сервер и воркер) и `NEXT_PUBLIC_SENTRY_DSN` (браузер), затем убедиться, что тестовое событие видно в проекте.
- **БЛОКЕР T08: нет удалённого репозитория.** CI-workflow написан, но «зелёный pipeline на PR» проверить негде. Нужно: создать репозиторий на GitHub, `git remote add origin <url>`, `git push -u origin main`, открыть PR. До этого T08 остаётся неотмеченной.
- Цикл работает через `/loop` в приложении. Отдельный скрипт `loop/run-loop.ps1` требует залогиненного CLI (не обязателен): standalone `claude` не залогинен — `claude -p` возвращает `"Not logged in · Please run /login"` (exit 1), поэтому `loop/run-loop.ps1` работать не может. Починка: открыть обычный терминал, выполнить `claude` → `/login`, либо задать `ANTHROPIC_API_KEY` в окружении. Проверить успех: `"Reply with exactly: OK" | claude -p --output-format json --max-turns 1` должен вернуть JSON с `is_error:false`.
  Подтверждено при пробнике: структура JSON корректна, поле `total_cost_usd` присутствует — cost-cap в скрипте будет работать сразу после логина.
- К сведению: окружение с TLS-перехватом — сетевые загрузки на этапе билда падают (`UNABLE_TO_VERIFY_LEAF_SIGNATURE`). Билд держим герметичным.
- Docker установлен (29.6.2), но контейнеры для T01 ещё не поднимались — проверить, что демон запущен.

## Журнал итераций

### 2026-08-12 · iter 35 (/loop) · T62 — done · PHASE 5 ЗАКРЫТА · ПЛАН ПРОЙДЕН
- Сделано: раздел `opportunity` в контракте C4, `reports.generateOpportunity`, форма ретейнера/часов/стоимости часа, раздел на публичной странице и в PDF.
- Вскрыто e2e: тип `owned` писался в общую таблицу `sources` по одному лишь домену — домен одного клиента становился «собственным» для чужого, и в аудит-отчёт попадала рекомендация обновить не его страницы. Владение теперь считается при чтении.
- Вскрыто прогоном всего сьюта: turbo запускал тесты пакетов параллельно над одной базой — корневой `pnpm test` переведён на `--concurrency=1`.
- Verify: 15 юнит + 7 интеграционных + 3 на неутекающий owned + e2e до PDF; 29 e2e и 484 юнит/интеграционных зелёные; typecheck/lint/build зелёные.
- Дальше: незакрытыми остаются только задачи, требующие действий человека (T08, T13–T15, T54).

### 2026-08-12 · iter 34 (/loop) · T61 — done
- Сделано: экран аудита (кнопка, шаги, авто-переход на диагностику), `runs.startAudit`, общая цепочка `completeRun` в @repo/pipeline.
- Вскрыто тестом: `classifyRunSources` не вызывался в приложении — диагностика получала домены без типа, а e2e был зелёным за счёт строк в глобальной таблице `sources` от интеграционных тестов. Урок (повтор T31): тесты, зависящие от глобального кэша, обязаны чистить его сами, иначе они проверяют чужие данные.
- Verify: 4 интеграционных теста цепочки + e2e от кнопки до diagnose; 28 e2e и 443 юнит/интеграционных зелёные; typecheck/lint/build зелёные.
- Дальше: T62 — последняя задача плана.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ); T54 (Stripe); доставка в Sentry не проверена (нет DSN).

### 2026-08-12 · iter 33 (/loop) · T60 — done
- Сделано: prospect-клиент (через существующий `clients.status`), генератор промптов в core, `prompts.generate/saveGenerated`, редактируемый черновик на экране измерения.
- Verify: 17 юнит + 7 интеграционных + e2e (23 промпта в 4 кластерах после правки); typecheck/lint/test зелёные.
- Решение: генерация не пишет в базу — сохраняется только то, что человек оставил в списке.
- Дальше: T61 (one-off audit run).
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ); T54 (Stripe); доставка в Sentry не проверена (нет DSN).

### 2026-08-12 · iter 32 (/loop) · T55 — done · T54 пропущена (блокер)
- Сделано: структурные JSON-логи, `ErrorReporter` (лог всегда, Sentry поверх при DSN), `onRequestError` и onError в tRPC, клиентский SDK за отдельным public-DSN, `global-error.tsx`, страница `/settings/usage` со стоимостью по клиентам и платформам.
- Verify: 5 тестов проводки ошибок + 5 интеграционных по суммам + e2e usage; 415 юнит/интеграционных и 26 e2e зелёные; typecheck/lint/build зелёные; смоук-запуск воркера показал JSON-логи.
- Решение: деньги суммируются в целых микродолларах — `Number` копит ошибку двоичной дроби на тысячах ответов.
- T54 (Stripe) пропущена: нужен аккаунт, Price ID и webhook secret. Дальше: T60 (Phase 5).
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ); T54 (Stripe); доставка в Sentry не проверена (нет DSN).

### 2026-08-12 · iter 31 (/loop) · T53 — done
- Сделано: `GET /api/reports/[id]/pdf` — печать публичной страницы отчёта headless-Chromium в storage, кнопка Download PDF в списке отчётов.
- Verify: e2e — 200/`application/pdf`, файл > 20 KB, извлечённый pdf-parse текст содержит имя клиента и агентства; чужой отчёт даёт 404. 25 e2e и 384 юнит-теста зелёные; typecheck/lint/build зелёные.
- Вскрыто e2e: `@types/pdf-parse` (v1) описывали default-функцию, которой в установленной v2 нет — typecheck был зелёным на коде, падавшем в рантайме. Урок: сторонние `@types/*` не совпадающей мажорной версии врут молча; типы пакета всегда предпочтительнее.
- Дальше: T54 и T55 помечены `[H]` — нужны ключи Stripe и Sentry DSN. Без них следующая доступная задача — T60 (Phase 5).
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ); T54 (Stripe), T55 (Sentry DSN).

### 2026-08-11 · iter 29 (/loop) · T46 — done · PHASE 3 ЗАКРЫТА · ПАУЗА
- Сделано: экран эксперимента (оценка, confidence, evidence, таймлайн, график), grep-тест инварианта 2.
- Verify: 19 e2e и 367 юнит-тестов зелёные; typecheck/lint/build зелёные.
- Grep-тест нашёл два вхождения запрещённых слов в комментариях — переписаны.
- Цикл остановлен по просьбе пользователя после закрытия Phase 3. Возобновить: /loop с тем же промптом.
- Дальше при возобновлении: T50 (Report generator).
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 28 (/loop) · T45 — done
- Сделано: experiments/math.ts (контракт C5), эвристика confidence с evidence-строками.
- Verify: пример спека (+14 pp, medium) воспроизводится; 362 теста; typecheck/lint/build зелёные.
- Тест поправил шкалу: недобор выборки теперь штрафует, а не просто не добавляет баллов.
- Дальше: T46 — последняя задача Phase 3.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 27 (/loop) · T44 — done
- Сделано: детект first_new_citation и visibility_change, встроен в aggregateClient.
- Verify: 16 тестов; 338 суммарно; typecheck/lint/build зелёные.
- Вскрыто тестом: окно «после» нельзя ограничивать текущим моментом (расхождение часов) — сделано открытым.
- Дальше: T45.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 26 (/loop) · T43 — done
- Сделано: схема экспериментов, planExperiment/baseline в core, роутер experiments, активная кнопка в диалоге.
- Verify: baseline 20.4% сходится с ручным расчётом; 322 теста; typecheck/lint/build/e2e зелёные.
- Урок (повтор): интеграционные тесты над общей БД нужно сериализовать — включено и для apps/web.
- Дальше: T44.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 25 (/loop) · T42 — done
- Сделано: доска действий (колонки, карточки, drawer), диалог предложения эксперимента при завершении.
- Verify: e2e зелёный; 19 e2e и 302 юнит-теста; typecheck/lint/build зелёные.
- Отклонение: e2e проверяет кнопки перемещения, а не HTML5-drag — перетаскивание в Playwright нестабильно.
- Дальше: T43.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 24 (/loop) · T41 — done
- Сделано: activity_log, запись событий из действий и прогонов, лента на обзоре клиента.
- Verify: 6 тестов журнала; 302 теста суммарно; typecheck/lint/build/e2e зелёные.
- Дальше: T42.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 23 (/loop) · T40 — done
- Сделано: схема actions, роутер actions.*, конвертация рекомендации в действие, активная кнопка на Diagnose.
- Verify: 9 юнит-тестов роутера + e2e; 296 тестов суммарно; typecheck/lint/build/e2e зелёные.
- Дальше: T41.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 22 (/loop) · T34 — done · PHASE 2 ЗАКРЫТА
- Сделано: экран Diagnose (донат, таблица присутствия, вывод, рекомендации), ссылка с обзора клиента.
- Verify: 18 e2e зелёные; typecheck/lint/build зелёные.
- Найдено тестом: у хорошо покрытого клиента рекомендаций ноль — это правильное поведение, тест разведён на два случая.
- Дальше: Phase 3 (Actions + Experiments), T40.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 21 (/loop) · T33 — done
- Сделано: три правила рекомендаций с обязательным reason (enforced zod-схемой), роутер diagnosis.recommendations.
- Verify: 16 юнит-тестов, включая инвариант «рекомендация без reason не собирается»; 287 тестов суммарно.
- Дальше: T34 — последняя задача Phase 2.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 20 (/loop) · T32 — done
- Сделано: copy.ts с утверждёнными формулировками, диагностика (mix/influential/gap/statement), роутер diagnosis.sourceGraph.
- Verify: пример из спека (16/25 vs 4/25) воспроизводится; 271 тест; typecheck/lint/build зелёные.
- Урок: интеграционные тесты над общей БД нельзя гонять параллельно — включена сериализация в pipeline.
- Дальше: T33.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 19 (/loop) · T31 — done
- Сделано: SourceClassifier с эвристической реализацией, кэш в памяти и постоянный кэш в таблице sources.
- Verify: спай подтверждает отсутствие повторных вызовов; 250 тестов суммарно; typecheck/lint/build зелёные.
- Урок (повтор старой ошибки): глобальные таблицы-кэши нужно чистить в тестах явно — иначе тесты зависят от порядка.
- Дальше: T32.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 18 (/loop) · T30 — done
- Сделано: словарь доменов и rule-классификатор в core, таблицы sources/citation_sources/source_presence, привязка цитат к источникам.
- Verify: 30 юнит + 6 интеграционных тестов; 234 теста суммарно; typecheck/lint/build зелёные.
- Дальше: T31.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 17 (/loop) · T25 — done · PHASE 1 ЗАКРЫТА
- Сделано: сквозной e2e золотого пути со сверкой цифры на обзоре против сырых ответов.
- Verify: 16 e2e зелёные, 198 юнит-тестов, typecheck/lint/build зелёные.
- Вскрыто и починено: count() в Playwright не ждёт загрузки — тест снимал счёт с пустой страницы.
- Дальше: Phase 2 (Diagnosis), T30.
- Блокеры: T08 (нет remote); T13–T15 (ключи платформ).

### 2026-08-11 · iter 16 (/loop) · T24 — done
- Сделано: роутер measurement.visibility, обзорный экран с карточками и графиком (recharts), реструктуризация маршрутов клиента.
- Verify: 15 e2e зелёные; typecheck/lint/build зелёные.
- Дальше: T25 — последняя задача Phase 1.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 15 (/loop) · T23 — done
- Сделано: highlightMentions в core (12 тестов), роутер runs.responses, экран сырых ответов с подсветкой и цитатами.
- Verify: 14 e2e зелёные; 198 юнит-тестов; typecheck/lint/build зелёные.
- Найдено тестом в продукте: прогон без расписания измерял только одну платформу — исправлено на весь запускной набор.
- Дальше: T24.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 14 (/loop) · T22 — done
- Сделано: @repo/pipeline (вынос оркестрации из воркера), роутер runs, панель расписания с Run now и поллингом статуса.
- Verify: 13 e2e зелёные, прогон доходит до done; 186 юнит-тестов; typecheck/lint/build зелёные.
- Решение: ручной запуск inline только в mock-режиме; в live обязан идти через очередь (комментарий в коде, привязка к T13–T15).
- Дальше: T23.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 13 (/loop) · T21 — done
- Сделано: парсер CSV в core (24 теста), роутер prompts, экран измерения с импортом.
- Verify: 11 e2e зелёные, включая импорт 10 строк в 2 кластера; 186 тестов суммарно.
- Вскрыто и починено: буквальный BOM в регулярке ронял lint; неоднозначный текстовый локатор в e2e.
- Дальше: T22.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 12 (/loop) · T20 — done
- Сделано: usage_counters с атомарным инкрементом, PLAN_LIMITS в core, роутер billing.usage.
- Verify: счётчик +18 на прогон, 20 параллельных инкрементов = 20; 162 теста суммарно; typecheck/lint/build зелёные.
- Дальше: T21.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 11 (/loop) · T19 — done
- Сделано: математика visibility (контракт C3), таблица visibility_snapshots, AggregateJob с идемпотентным upsert.
- Verify: 16 юнит + 5 интеграционных тестов; всего 146 юнит/интеграционных; typecheck/lint/build зелёные.
- Отклонение: NULLS NOT DISTINCT недоступен в drizzle-kit 0.30 — идемпотентность в коде, не в БД.
- Дальше: T20.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 10 (/loop) · T18 — done
- Сделано: парсер упоминаний (контракт C2) и раскладка citations, ParseJob в воркере.
- Verify: 36 тестов парсера + 5 интеграционных; всего 125 юнит/интеграционных, 8 e2e; typecheck/lint/build зелёные.
- Вскрыто и починено: тесты воркера копили данные в общей БД и роняли тест seed через порядок запуска. Урок: интеграционные тесты обязаны убирать за собой (afterEach), а проверки — быть узкими, а не глобальными count.
- Дальше: T19.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 9 (/loop) · T17 — done
- Сделано: оркестрация прогона, очереди по платформам с лимитерами, запись ответов и сырых текстов в storage.
- Verify: 17 тестов воркера зелёные (18 ответов, статус done, защита от дубля); смоук-запуск процесса.
- Вскрыто смоук-запуском: BullMQ 6 не принимает ':' в имени очереди — typecheck и lint это не ловят. Урок: после изменений в воркере запускать процесс, а не только тесты.
- Дальше: T18.
- Блокеры: T08 (нет remote); ключи для T13–T15.

### 2026-08-11 · iter 8 (/loop) · T16 — done
- Сделано: BullMQ-воркер, очереди, повторяемый тик планировщика, graceful shutdown.
- Verify: 8 тестов (включая round-trip к Redis), проверен старт процесса; typecheck/lint/test/build зелёные.
- Дальше: T17.
- Блокеры: T08 (нет remote); ключи платформ для T13–T15 (не блокируют — план разрешает отложить до конца Phase 1).

### 2026-08-11 · iter 7 (/loop) · T11, T12 — done
- Сделано: контракт C1 + 9 fixtures (T11); MockAdapter с детерминированным выбором и реестр адаптеров (T12).
- Verify: 48 тестов в core зелёные; typecheck/lint/test/build зелёные.
- Дальше: T13–T15 требуют API-ключей (пометка [H]); без ключей их live-часть не проверить — переходить к T16.
- Блокеры: T08 (нет remote); ключи платформ для T13–T15.

### 2026-08-11 · iter 6 (/loop) · T10 — done
- Сделано: схема measurement (7 таблиц, миграция 0002), seed с кластерами/промптами/расписанием, 4 новых теста.
- Verify: 8 тестов db зелёные, seed идемпотентен, типы экспортированы; typecheck/lint/test/build зелёные.
- Дальше: T11 (fixtures).
- Блокеры: только T08 (нет remote).

### 2026-08-11 · iter 5 (/loop) · T04 — done
- Сделано: tRPC-каркас, protectedProcedure/roleProcedure/assertTenant, роутеры agency и clients, приглашения с токеном, /invite/[token], React-провайдер tRPC.
- Verify: 10 юнит-тестов зелёные, включая проверку неразличимости чужого и несуществующего ресурса; typecheck/lint/build/e2e зелёные.
- Вскрыто и починено: vite-tsconfig-paths ESM-only (алиас вручную), zod выровнен до v4.
- Дальше: T05.
- Блокеры: нет.

### 2026-08-11 · iter 4 (/loop, dynamic) · T03 — done
- Сделано: Better Auth email+password поверх `users`, таблицы сессий/аккаунтов/верификаций/приглашений, страницы login/signup, защищённый /dashboard, Playwright-конфиг и первый e2e.
- Verify: `pnpm e2e` 2/2 зелёные; typecheck/lint/build зелёные, юнит-тесты T02 не сломались.
- Вскрыто и починено: ключи schema у drizzle-адаптера должны совпадать с `modelName`; `drizzle-orm` утекал в apps/web (вынесено в `@repo/db/queries`); Next выбирал корнем чужой lockfile (`outputFileTracingRoot`); зависший сервер на 3100 переиспользовался Playwright'ом и маскировал исправления.
- Дальше: T04.
- Блокеры: нет. Цикл работает в приложении (`/loop`), логин CLI нужен только для внешнего скрипта.

### 2026-08-11 · iter 3 (ручной прогон) · T02 — done
- Сделано: схема tenancy + первая миграция + идемпотентный seed + 4 интеграционных теста.
- Verify: двойной `pnpm db:seed` без дублей, `pnpm --filter @repo/db test` 4/4; typecheck/lint/test/build зелёные.
- Вскрыто и починено: drizzle-kit не резолвит ESM-расширения `.js` (импорты без расширений, правило в CLAUDE.md).
- Дальше: T03.
- Блокеры: цикл заблокирован логином CLI (см. выше).

### 2026-08-11 · iter 2 (ручной прогон) · T01 — done
- Сделано: docker-compose (Postgres 16, Redis 7, healthchecks), Drizzle + postgres.js, `.env.example`, программный мигратор, `db:check`.
- Verify: `docker compose up -d && pnpm db:migrate && pnpm db:check` зелёные; typecheck/lint/test/build зелёные и без warning'ов.
- Вскрыто и починено: dotenv не видел корневой `.env` из папки пакета; `next-env.d.ts` ронял lint; turbo шумел «no output files» (добавлены package-level turbo.json).
- Дальше: T02.
- Блокеры: цикл всё ещё заблокирован логином CLI (см. выше).

### 2026-08-11 · iter 1 (ручной прогон) · T00 — done
- Сделано: монорепо pnpm+Turborepo (apps/web, apps/worker, packages/core, packages/db), общие tsconfig/eslint/prettier, Tailwind v4 с дизайн-токенами из плана §4.3.
- Verify: `pnpm install && pnpm build && pnpm lint` зелёные, дополнительно `pnpm typecheck` и `pnpm test`.
- Вскрыто ручным прогоном (и починено): (1) `next/font/google` ломает билд из-за TLS-перехвата → перешли на font-stack, правило записано в CLAUDE.md; (2) `Get-Content`/`Set-Content` в PowerShell 5.1 портят UTF-8 в русских доках → правило записано в CLAUDE.md; (3) структура репозитория: спеки в `docs/`, операционные файлы в корне.
- Дальше: T01.
- Блокеры: нет.

<!-- новые записи добавлять СВЕРХУ, формат:
### 2026-08-12 · iter 3 · T02 — done
- Сделано: схема tenancy + seed, verify прошёл (pnpm db:seed идемпотентен, юнит-тест зелёный)
- Дальше: T03 auth
- Блокеры: нет
-->
