Ты выполняешь ОДНУ итерацию имплементационного цикла проекта Citeworthy.

Порядок действий, строго:

1. Прочитай `loop/GOAL.md` (контракт и запреты), `loop/STATE.md` (где мы), `CLAUDE.md` (инварианты), затем найди в `TASKS.md` ПЕРВУЮ неотмеченную задачу `- [ ]` (задачи `[H]` в live-части пропускай — делай их mock-часть, если она описана, иначе переходи к следующей).

2. Выполни ТОЛЬКО эту одну задачу. Следуй контрактам C1–C6 из TASKS.md дословно.

3. Прогони verify-критерий задачи + `pnpm typecheck && pnpm lint && pnpm test` (если монорепо уже существует). Чини до зелёного. Если за разумное число попыток починить не удаётся — НЕ отмечай чекбокс, опиши блокер в `loop/STATE.md` и заверши итерацию.

4. При зелёном verify:
   - отметь задачу `[x]` в TASKS.md (только чекбокс, текст задачи не менять);
   - добавь строку в секцию Log в конце TASKS.md;
   - обнови `loop/STATE.md` (запись сверху журнала: что сделано, что дальше, блокеры);
   - сделай ОДИН git-коммит: `T<NN>: <краткое описание>`.

5. Заверши ход. Не начинай следующую задачу.

Запреты из GOAL.md действуют без исключений. Если все задачи отмечены — напиши "ALL TASKS DONE" и ничего не делай.
