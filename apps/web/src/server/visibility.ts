/**
 * Видимость клиента за окно: матрица, заметность и движение.
 *
 * Одно определение на экран, на публичный API и на пересчёт возможностей —
 * оно живёт в `@repo/pipeline/read-models`. Второе определение той же цифры
 * рано или поздно разошлось бы с первым, и агентство увидело бы в своём
 * дашборде не то, что видит у нас.
 */
export { clientVisibility } from "@repo/pipeline/read-models";
