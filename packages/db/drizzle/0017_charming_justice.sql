-- Присутствие в источниках никогда не записывалось: строк в таблице ноль,
-- а `upsertSourcePresence` не вызывался ниоткуда. Проверка результата работы
-- считает присутствие по датированным фактам цитирования — по тем же ответам,
-- что и всё остальное измерение.
--
-- Пустая таблица, выглядящая как данные, однажды будет принята за данные.
DROP TABLE "source_presence" CASCADE;
