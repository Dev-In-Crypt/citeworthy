/**
 * @repo/db — Drizzle-схема и миграции. Единственное место определения таблиц.
 * Схема появляется в T02 (tenancy), T10 (measurement), T30 (sources), T40/T43, T50.
 */

export const DB_PACKAGE_NAME = "@repo/db";
