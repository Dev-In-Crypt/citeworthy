# TASKS.md — план исполнения для АИ-кодера

> Атомарные задачи в порядке исполнения. Одна задача = одна сессия / один PR.
> Архитектура, схема БД и дизайн экранов — в [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md).
> Инварианты проекта — в [CLAUDE.md](CLAUDE.md). Читать оба перед любой задачей.
>
> Формат задачи: **цель → затрагиваемые пакеты → verify** (исполняемая проверка; задача не считается сделанной, пока verify не проходит).
> Задачи с пометкой `[H]` требуют действий человека (ключи, аккаунты) — см. раздел «Human checklist» внизу; до их выполнения соответствующие интеграции работают на mock'ах.

---

## Контракты (зафиксированы, не изобретать заново)

### C1. PlatformAdapter (`packages/core/src/adapters/types.ts`)

```ts
export type Platform = 'chatgpt' | 'perplexity' | 'gemini';

export interface AdapterResult {
  text: string;
  citations: { url: string; title?: string }[];
  modelVersion: string;
  costUsd: number;
  latencyMs: number;
}

export interface PlatformAdapter {
  platform: Platform;
  execute(prompt: string, opts?: { geo?: string; lang?: string }): Promise<AdapterResult>;
}
```

### C2. ParsedResponse — выход парсера mentions/citations (`packages/core/src/parsing/types.ts`)

```ts
export interface ParsedResponse {
  mentions: {
    name: string;              // каноническое имя из brand_names/competitor_names
    position: number;          // 1-based порядок появления в ответе
    sentiment: 'positive' | 'neutral' | 'negative';
    isClient: boolean;
    isCompetitor: boolean;
  }[];
  citationUrls: { url: string; title?: string }[];
}
```

### C3. Visibility

`visibility_pct = responses_with_client_mention / total_responses * 100` за период, по всем sample'ам. Агрегируется в разрезах: client × [cluster|null] × [platform|null] × period (неделя). Никогда не считать по одному ответу.

### C4. ReportPayload (`packages/core/src/reports/schema.ts`, zod)

```ts
export const ReportPayload = z.object({
  client: z.object({ name: z.string() }),
  period: z.object({ start: z.string(), end: z.string() }), // ISO dates
  visibility: z.object({ before: z.number(), after: z.number() }),
  competitorGap: z.object({ before: z.number(), after: z.number() }), // pp, знак минус = отставание
  workCompleted: z.array(z.object({ label: z.string(), count: z.number() })),
  results: z.object({
    newCitedUrls: z.number(),
    newBrandMentions: z.number(),
    visibilityDeltaPp: z.number(),
  }),
  highestImpactAction: z.object({
    title: z.string(),
    estimatedContribution: z.string(), // напр. "+4–6 pp", всегда "estimated"
    confidence: z.enum(['low', 'medium', 'high']),
  }).nullable(),
  nextSprint: z.array(z.string()),
});
```

### C5. Experiment math

`incremental_pp = (treatment_after − treatment_before) − (control_after − control_before)`.
Confidence-эвристика (см. задачу T45): начисление баллов за sample size, наличие new citation после action_date, концентрацию lift в affected clusters, стабильность control; маппинг баллов → low/medium/high.

### C6. tRPC-роутеры

Имена и сигнатуры — по разделу 3.5 IMPLEMENTATION_PLAN.md. Все процедуры кроме `publicReport.*` и `auth.*` — protected, с tenancy guard.

---

## Phase 0 — Foundation

- [x] **T00. Монорепо-скелет.** pnpm workspaces + Turborepo: `apps/web` (Next.js 15, TS, Tailwind, shadcn/ui init), `apps/worker` (пустой TS-процесс), `packages/db`, `packages/core`. Общие tsconfig/eslint/prettier.
  Verify: `pnpm install && pnpm build && pnpm lint` зелёные из корня.

- [x] **T01. Локальная инфраструктура.** `docker-compose.yml` с Postgres 16 и Redis 7; `packages/db`: Drizzle настроен, скрипты `db:generate`, `db:migrate`, `db:check`, `db:studio`; `.env.example` со всеми переменными.
  Verify: `docker compose up -d && pnpm db:migrate && pnpm db:check` проходит на чистой БД (`db:check` обязателен — `db:migrate` при пустой схеме не подключается к БД и сам по себе ничего не доказывает).

- [x] **T02. Схема tenancy.** Таблицы `agencies`, `users`, `clients` по разделу 2.1 плана + миграция + seed-скрипт (`pnpm db:seed`: 1 агентство, 1 owner, 2 клиента с brand/competitor names).
  Verify: `pnpm db:seed` идемпотентен; юнит-тест на select seed-данных.

- [x] **T03. Auth.** Better Auth (email+password) в `apps/web`: signup создаёт agency+owner, login, session; страницы `/login`, `/signup`.
  Verify: Playwright-тест signup→logout→login.

- [x] **T04. tRPC + tenancy guard + RBAC.** tRPC-каркас, `protectedProcedure` кладёт `agencyId` в контекст; helper `assertTenant(resource.agencyId)`; роли owner/admin/member; invite по email-токену (`/invite/[token]`, отправка писем — mock в dev: лог в консоль).
  Verify: юнит-тест — запрос к чужому clientId возвращает NOT_FOUND (не FORBIDDEN, не утечка существования).

- [x] **T05. UI shell.** Layout с сайдбаром (Clients, Settings), топбар с agency-именем, тема по разделу 4.3 (slate + indigo, Inter, tabular-nums для метрик).
  Verify: скриншот-прогон Playwright, все роуты рендерятся без ошибок консоли.

- [x] **T06. Agency settings.** `/settings`: профиль, загрузка логотипа (S3-совместимый storage, в dev — локальный MinIO в docker-compose или файловая заглушка), brand_color picker, список команды + invite.
  Verify: e2e — загрузка png-логотипа и смена цвета сохраняются и переживают reload.

- [x] **T07. Clients CRUD.** Роутер `clients.*` + страницы: список клиентов (карточки-заглушки метрик), создание/редактирование (name, domain, industry, brand_names[], competitor_names[]).
  Verify: e2e — создать клиента, добавить 2 alias бренда и 3 конкурентов, увидеть в списке.

- [ ] **T08. CI.** GitHub Actions: lint, typecheck, unit-тесты, build; Postgres/Redis как services для тестов.
  Verify: pipeline зелёный на PR.

## Phase 1 — Measurement

- [x] **T10. Схема measurement.** Таблицы раздела 2.2 плана: `prompt_clusters`, `prompts`, `run_schedules`, `runs`, `responses` (+ `mentions`, `citations` — пустые пока) + миграция + расширение seed (2 кластера × 5 промптов, 1 из них `is_control`).
  Verify: миграция + seed проходят; drizzle-типы экспортированы из `packages/db`.

- [x] **T11. Fixture-ответы платформ.** `packages/core/fixtures/`: по 3 реалистичных ответа на платформу (текст с упоминаниями брендов + citations в родном формате платформы), включая edge-cases: бренд не упомянут; бренд под alias; ответ без citations.
  Verify: fixtures валидируются против типа `AdapterResult`.

- [x] **T12. MockAdapter + реестр адаптеров.** Контракт C1 в `packages/core`; `MockAdapter` отдаёт fixtures детерминированно (по хэшу промпта); реестр `getAdapter(platform)`; env-флаг `ADAPTERS_MODE=mock|live`.
  Verify: юнит-тесты MockAdapter; весь дальнейший пайплайн до T25 работает в mock-режиме.

- [ ] **T13. `[H]` OpenAI adapter.** Responses API с web search tool; маппинг annotations → citations; подсчёт costUsd из usage; retry ×3 с backoff на 429/5xx.
  Verify: юнит-тест на маппинг из записанного сырого ответа API (fixture); smoke-скрипт `pnpm adapter:smoke chatgpt "best CRM for startups"` (запускается только при наличии ключа).

- [ ] **T14. `[H]` Perplexity adapter.** Sonar API, citations из ответа, cost, retry.
  Verify: аналогично T13.

- [ ] **T15. `[H]` Gemini adapter.** Google Search grounding, groundingMetadata → citations, cost, retry.
  Verify: аналогично T13.

- [x] **T16. Worker-скелет.** `apps/worker`: BullMQ, подключение к Redis, очереди `runs`, `parse`, `aggregate`; graceful shutdown; repeatable job — cron-тик раз в 5 мин ищет `run_schedules.next_run_at <= now`.
  Verify: интеграционный тест — тик подхватывает due-schedule и создаёт `run`.

- [x] **T17. Run-оркестрация.** Создание run → fan-out job'ов (prompt × platform × sample, `samples_per_prompt` default 3) → каждый job вызывает адаптер, пишет `responses` (raw в storage, метаданные+cost в PG) → по завершении всех — enqueue parse. Rate-limit per platform (BullMQ limiter). Статусы run: pending/running/done/failed(partial).
  Verify: интеграционный тест в mock-режиме — schedule на 2 промпта × 3 платформы × 3 sample даёт ровно 18 responses и run.status=done.

- [x] **T18. ParseJob.** Контракт C2: LLM-экстракция (structured output, дешёвая модель, в тестах — mock LLM) + детерминированный alias-fallback (case-insensitive поиск brand_names/competitor_names). Запись в `mentions`/`citations`.
  Verify: юнит-тесты на всех fixtures из T11: клиент найден под alias; не-упоминание не даёт false positive; citations извлечены полностью. Это самый тестируемый код проекта.

- [x] **T19. AggregateJob.** Контракт C3 → `visibility_snapshots` (client × cluster × platform × неделя + свёртки с null). Пересчёт идемпотентен (upsert по ключу разреза).
  Verify: юнит-тест: подготовленный набор responses → известные вручную посчитанные проценты; повторный прогон не меняет результат.

- [x] **T20. Usage counters.** `usage_counters`: инкремент ai_checks на каждый response; роутер `billing.usage`.
  Verify: юнит-тест — run из T17 увеличивает счётчик на 18.

- [x] **T21. UI: кластера и промпты.** Экран `/clients/[id]/measure`: CRUD кластеров (name, intent) и промптов (text, is_control), CSV-импорт (колонки: cluster,intent,prompt,is_control).
  Verify: e2e — импорт CSV на 10 строк создаёт 2 кластера и 10 промптов.

- [x] **T22. UI: расписание и ручной запуск.** Настройка schedule (cadence, платформы, samples) + кнопка «Run now» → `runs.triggerManual` → живой статус run'а (поллинг).
  Verify: e2e в mock-режиме — Run now доходит до done, responses видны.

- [x] **T23. UI: drill-down ответов.** Кластер → промпт → история ответов по платформам: полный текст, подсветка mentions (клиент зелёным, конкуренты оранжевым), список citations.
  Verify: e2e — на seed+mock данных подсветка присутствует у известного fixture-ответа.

- [x] **T24. UI: visibility chart.** Client overview `/clients/[id]`: stat-карточки (visibility, gap, open actions=0 пока, last report=—) + line chart клиент vs конкуренты, фильтр платформы/кластера (Recharts).
  Verify: e2e — график рендерит данные из visibility_snapshots.

- [x] **T25. E2e золотой путь measurement.** Playwright: create client → import prompts → run (mock) → drill-down → chart. Закрепляет Phase 1.
  Verify: тест зелёный в CI.

## Phase 2 — Diagnosis

- [x] **T30. Схема sources + rule-классификатор.** Таблицы `sources`, `citation_sources`, `source_presence`; rule-based классификация по домену: client domain→owned, известные review/directory/UGC-домены (стартовый словарь в `packages/core/src/sources/domains.ts`), остальное→null (для LLM).
  Verify: юнит-тесты на словарь и на owned-матчинг по client.domain.

- [x] **T31. LLM-классификатор источников.** Для доменов с type=null: LLM по domain+title (mock в тестах), результат кэшируется в `sources` (домен классифицируется один раз глобально). Встроить в parse-пайплайн после T18.
  Verify: юнит-тест — повторная встреча домена не вызывает LLM (спай на клиенте).

- [x] **T32. Diagnosis service.** `diagnosis.sourceGraph(clientId, clusterId)`: source mix (доли типов), influential sources (топ-N по частоте цитирования: домен, тип, freq, client_present, competitors_present), текстовый gap-statement (template + LLM-суммаризация, mock в тестах). Presence в MVP — по mentions в тех же ответах, где источник цитировался.
  Verify: юнит-тест на подготовленном датасете воспроизводит пример из спека (16/25 vs 4/25 → вывод «gap создаётся third-party sources»).

- [x] **T33. Recommendations.** Rule-based генератор кандидатов (правила из раздела 3.2 плана), каждый обязан иметь непустой `reason`; роутер `diagnosis.recommendations`.
  Verify: юнит-тесты по одному на каждое правило; тест-инвариант: рекомендация без reason невозможна (тип/zod).

- [x] **T34. UI: Diagnose.** `/clients/[id]/diagnose`: донат source mix, таблица influential sources (чипы конкурентов, ✓/✗ клиента, кнопка «Create action» — пока disabled до T40), diagnosis-блок, sankey (cluster → source types → top sources).
  Verify: e2e на seed-данных — таблица и mix совпадают с ответом API.

## Phase 3 — Actions + Experiments

- [x] **T40. Actions.** Схема 2.4 (`actions`) + роутер `actions.*` + convert-from-recommendation (заполняет title/reason/type/source/clusters). Включить кнопку из T34.
  Verify: юнит-тест convert; e2e — рекомендация превращается в action одним кликом.

- [x] **T41. Activity log.** `activity_log` + запись событий: action created/status changed/completed, run finished, report generated. Feed на client overview.
  Verify: юнит-тест — смена статуса action создаёт запись.

- [x] **T42. UI: Actions board.** `/clients/[id]/actions`: kanban (Backlog/In progress/Done) + таблица; карточка и drawer по разделу 4.2; при переводе в Done — диалог «Create experiment from this action?».
  Verify: e2e — drag в Done открывает диалог.

- [x] **T43. Experiments: создание.** Схема (`experiments`, `experiment_events`, `experiment_results`) + создание из action: baseline autofill (14–28 дней до action_date из snapshots), treatment = affected_cluster_ids, control = prompts с is_control.
  Verify: юнит-тест — baseline числа совпадают с ручным расчётом по seed-snapshots.

- [x] **T44. Timeline auto-events.** В AggregateJob: для активных экспериментов детект `first_new_citation` (новый URL в affected clusters с датой > action_date) и `visibility_change` (пересечение порога Δ); запись в `experiment_events`.
  Verify: интеграционный тест — подложенный «новый» citation после action_date порождает ровно одно событие.

- [x] **T45. Experiment math + confidence.** Контракт C5 в `packages/core/src/experiments/math.ts` (чистые функции): incremental_pp + балльная эвристика confidence + сборка evidence list.
  Verify: table-driven юнит-тесты, включая пример из спека (18→34 vs 21→23 ⇒ +14 pp, medium).

- [x] **T46. UI: Experiment timeline.** `/clients/[id]/experiments`: список + детальный экран — вертикальный timeline событий, сдвоенный chart Treatment/Control с пунктиром action date, result-блок («Estimated incremental effect», confidence-бейдж, evidence). Формулировки — только из copy-констант (см. CLAUDE.md).
  Verify: e2e на seed-эксперименте; grep-тест: в bundle отчётных/экспериментных экранов нет слов proven/guaranteed/caused.

## Phase 4 — Reporting + Billing

- [x] **T50. Report generator.** Контракт C4: сборка payload из snapshots/actions/experiments за период + юнит-тесты; `reports.generate/get`; хранение в `reports.payload`.
  Verify: юнит-тест — payload на seed-данных валиден по zod и числа совпадают с ручным расчётом.

- [x] **T51. Public report.** `/r/[token]` (SSR, без auth): секции по разделу 4.2, полный white-label (лого + brand_color агентства, ноль упоминаний продукта), print-CSS.
  Verify: e2e — страница открывается в чистой (незалогиненной) сессии; grep рендера на название продукта = 0 вхождений.

- [ ] **T52. Approve by link.** Кнопка Approve на `/r/[token]` → `approved_at`, `approved_by_name` (поле ввода имени); статус виден агентству.
  Verify: e2e — approve из анонимной сессии отражается в приложении.

- [ ] **T53. PDF.** Endpoint `reports.exportPdf`: Playwright print-to-PDF той же страницы → storage; кнопка Download.
  Verify: интеграционный тест — PDF генерируется, размер > 20KB, содержит имя клиента (pdf-parse).

- [ ] **T54. `[H]` Stripe.** Checkout (3 плана), customer portal, webhook → `subscriptions`; enforcement: превышение client_limit блокирует создание клиента с понятной ошибкой; AI-check allowance — мягкий лимит (warning) в MVP.
  Verify: интеграционный тест с stripe-mock/test mode — webhook меняет план и лимит применяется.

- [ ] **T55. `[H]` Observability.** Sentry (web+worker), структурные логи воркера, внутренняя страница `/settings/usage`: cost_usd по клиентам/платформам за период.
  Verify: тестовое исключение доезжает до Sentry (в dev — до консольного транспорта); usage-страница показывает суммы, совпадающие с SUM(cost_usd).

## Phase 5 — Free Opportunity Audit (founder tool)

- [ ] **T60. Audit mode: генерация промптов.** Клиент помечается `is_prospect`; действие «Generate buyer prompts»: LLM по domain+industry → 20–30 промптов с intent, редактируемый список до сохранения.
  Verify: e2e с mock-LLM — генерация, правка, сохранение в кластера.

- [ ] **T61. One-off audit run.** Кнопка «Run audit»: один run по всем платформам → авто parse/classify/aggregate → авто-diagnosis. Прогресс-экран.
  Verify: e2e в mock-режиме — от кнопки до готового diagnose-экрана без ручных шагов.

- [ ] **T62. Opportunity Report.** Расширение ReportPayload полями `opportunity` (current visibility, competitor avg, ranked actions top-20, 90-day scope, suggested retainer, estimated effort, margin — значения retainer/effort вводятся вручную с дефолтами $3,500 / 8–12 h) + white-label PDF.
  Verify: юнит-тест payload; e2e — PDF аудита генерируется для prospect-клиента.

---

## Human checklist `[H]` (нужно от фаундера, код на этих задачах не блокируется — работает mock)

| Когда | Что |
|---|---|
| до T13–T15 live-режима | API-ключи: OpenAI, Perplexity, Google AI (env: `OPENAI_API_KEY`, `PERPLEXITY_API_KEY`, `GEMINI_API_KEY`) |
| до T54 | Stripe-аккаунт, 3 Price ID, webhook secret |
| до T55 | Sentry DSN |
| до деплоя | Vercel-проект, Railway/Fly для worker+Redis, prod Postgres, S3-bucket, домен |
| постоянно | Outreach по Stage 0 (30 агентств) — вне кода |

## Порядок и зависимости

- Фазы строго последовательны; внутри фазы задачи идут по номерам, кроме независимых пар: T13/T14/T15 (параллельны, после T12), T21–T24 (после T19, между собой почти независимы), T51/T53 (после T50).
- Live-адаптеры (T13–T15) можно отложить до конца Phase 1: всё тестируется в `ADAPTERS_MODE=mock`.
- После каждой фазы: прогнать весь тест-сьют + e2e, обновить чекбоксы здесь, короткая запись в конец файла «Log» (дата, что сделано, что отложено).

## Log

- **2026-08-12 · T51 done.** Публичная страница `/r/[token]`: SSR без авторизации, логотип и цвет агентства, разделы по спеку §10 (видимость, разрыв, что сделано, результаты, следующий спринт). Единственный анонимный доступ в продукте — доступ даёт сам токен (32 байта), поэтому публичный роутер содержит ровно одну операцию, и та не может менять данные агентства. Неизвестный и просроченный токен дают одинаковый ответ, чтобы перебор не отличал одно от другого. Оговорки вынесены в блок «How to read this» на странице, а не в мелкий шрифт: клиент принимает решения по этим цифрам и должен понимать, чего они не значат. Страница — server-компонент почти без интерактива: чем меньше исполняемого кода на документе, который открывают по ссылке, тем меньше поводов ему не доверять. Verify: e2e открывает отчёт в чистом контексте без куки, проверяет брендинг агентства и **отсутствие любого следа продукта в разметке** (инвариант 3), включая заголовок вкладки; отдельная проверка — нет слов, обещающих причинность; битая ссылка объясняет проблему вместо ошибки.
- **2026-08-12 · T50 done.** (Задача была прервана блокером Docker и завершена после его снятия.) Сделано: контракт C4 (`reports/schema.ts`), чистый сборщик `buildReportPayload` с 17 тестами (числа сверены вручную: 23→31% видимости, разрыв −14→−6 pp), схема `reports`/`report_shares` и миграция 0009, запросы и роутер `reports.generate/share/list/get`. Решения: payload хранится целиком и не пересчитывается при просмотре — клиент видел конкретные цифры на конкретную дату; вклад подаётся диапазоном (`+3–7 pp`), а не точкой, потому что точное число выглядело бы как измеренная величина; пояснение о природе измерения и оговорки (нет группы сравнения, короткий период) добавляются в каждый отчёт автоматически. **Не выполнено:** миграция не применена и интеграционные тесты не прогнаны — Docker Desktop не поднимается после перезагрузки машины (WSL-дистрибутив `docker-desktop` в состоянии Stopped, перезапуск `wsl --shutdown` + рестарт приложения не помог). Требуется вмешательство человека.
- **2026-08-11 · T46 done. Phase 3 закрыта.** Экран эксперимента: оценка вклада, бейдж уверенности, список evidence, вертикальный таймлайн событий и сдвоенный график treatment/control с пунктиром на дате действия. Дисклеймер стоит рядом с цифрой, а не мелким шрифтом внизу страницы — если ограничение важно, оно должно читаться там же, где результат. **Grep-тест инварианта 2** сканирует все исходники `apps/web/src` и `packages/core/src` (кроме тестов) на слова, заявляющие доказанную причинность, и падает на любом вхождении; сами запрещённые слова собираются из кодов символов, иначе тест был бы своим же нарушением. Отдельная проверка «сканируется непустой набор файлов» — иначе тест был бы зелёным просто потому, что ничего не прочитал. Тест сразу нашёл два вхождения в комментариях, перечислявших запрещённые слова; комментарии переписаны через ссылку на инвариант. Verify: e2e проверяет заголовок оценки, бейдж уверенности, дисклеймер, evidence, таймлайн и график.
- **2026-08-11 · T45 done.** Контракт C5 в чистых функциях: `incrementalPp = Δtreatment − Δcontrol` и балльная эвристика уверенности. Verify выполнен буквально по примеру спека §6.7 — 18→34 против 21→23 даёт +14 pp и confidence medium; с появившейся новой цитатой поднимается до high. Эвристика намеренно простая и читаемая: агентство должно понимать, почему стоит «medium», а не доверять непрозрачной формуле, и каждый балл сопровождается строкой evidence. Уверенность выражается словом, а не числом с процентами — точность, которой нет, не должна выглядеть как точность. Без контроля возвращается сырой Δtreatment, но со штрафом: движение может целиком объясняться ростом самих платформ. Формулировка всегда «Estimated incremental effect», к результату приложен дисклеймер «это evidence, а не атрибуция причины»; отдельный тест проверяет отсутствие запрещённых слов во всех строках. **Тест поправил мою шкалу:** при трёх ответах после действия выходило «medium» — недобор выборки теперь штрафуется наравне с отсутствием контроля, потому что доля по трём ответам это не слабое измерение, а его отсутствие (инвариант 6). Verify: 24 table-driven теста.
- **2026-08-11 · T44 done.** Детект событий таймлайна встроен в агрегацию: появление источника, которого до действия в измерениях не было, и сдвиг видимости относительно baseline выше порога шума (5 pp). Событие — это наблюдение, а не вывод: «после действия появилась новая цитата» факт, «действие вызвало цитату» утверждение, которого продукт делать не имеет права. Домены схлопываются перед сравнением — иначе «новым» оказывался бы каждый повтор уже известного источника. Порог нужен, чтобы таймлайн, где «что-то произошло» каждую неделю, не переставал читаться; падение видимости фиксируется наравне с ростом — агентство должно узнать о нём первым. Детект идемпотентен: агрегация идёт после каждого прогона, и без защиты таймлайн за месяц стал бы потоком дублей. Verify: 11 юнит-тестов + 5 интеграционных, включая «ровно одно событие» и «повторный запуск дубля не создаёт». Починено по ходу: окно «после» ограничивалось текущим моментом и теряло свежий срез при расхождении часов между БД и процессом — интервал сделан открытым.
- **2026-08-11 · T43 done.** Схема экспериментов (миграция 0008) и создание из завершённого действия с автозаполнением baseline. Verify выполнен буквально: baseline совпадает с посчитанным вручную взвешенным средним (18%×20 + 22%×30) / 50 = 20.4%, срез вне окна в расчёт не попал. Ключевые решения: (1) baseline фиксируется в момент создания — через несколько недель период «до» уже не восстановить, данные смешаются; (2) среднее взвешено числом ответов, а не срезов — иначе неделя с тремя ответами весила бы как неделя с сотней; (3) граница окна полуоткрыта, день действия относится к «после»; (4) пустая группа даёт null, а не ноль — ноль читался бы как «клиента не упоминают»; (5) контроль — кластеры, которых действие не касалось, и когда таких нет, продукт **говорит об этом вслух** (`EXPERIMENT_WARNINGS.noControl`), а не делает вид, что сравнение корректно. Спек прямо предупреждает: у клиента один бренд, настоящей контрольной группы не существует — эта слабость обязана доходить до агентства. Verify: 14 юнит-тестов baseline + 6 интеграционных. Починено по ходу: тест импортировал drizzle прямо в apps/web (нарушение собственного инварианта) — добавлен helper `setActionCompletedAt` в @repo/db; интеграционные тесты web гонялись параллельно над одной БД и давали случайные падения — включена сериализация файлов, как ранее в pipeline.
- **2026-08-11 · T42 done.** Доска действий: три колонки, карточки с типом и оценкой эффекта, drawer с полным объяснением. `reason` вынесен в drawer на видное место, а не спрятан в подписи карточки — это то, что агентство перескажет клиенту. Перевод в Done открывает предложение создать эксперимент с объяснением, почему момент важен: baseline нельзя восстановить задним числом, когда пройдут ещё недели. Кнопка создания эксперимента неактивна до T43. Отклонение от плана: помимо drag-and-drop сделаны явные кнопки перемещения, и e2e проверяет именно их — перетаскивание HTML5 через Playwright нестабильно, а мигающий тест хуже, чем тест другого пути к тому же состоянию; кнопки заодно решают доступность с клавиатуры и узкие экраны. Verify: e2e — перемещение в In progress диалога не открывает, перевод в Done открывает, статус переживает reload, завершение попадает в журнал активности.
- **2026-08-11 · T41 done.** Журнал активности (миграция 0007) и лента на обзорном экране клиента. Запись идёт в момент события, а не восстанавливается задним числом по состоянию таблиц: состояние показывает «как сейчас», а агентству для отчёта нужно «что происходило за период». Завершение действия — отдельный тип события, а не просто смена статуса: именно оно попадёт в клиентский отчёт и станет точкой отсчёта для эксперимента. `logActivity` намеренно не пробрасывает исключение наружу — сбой записи в журнал не должен отменять уже выполненное действие. Повторный перевод в тот же статус журнал не засоряет. Verify: 6 тестов, включая «смена статуса создаёт запись» и порядок ленты; в e2e обзорного экрана добавлена проверка, что завершённый прогон виден в ленте.
- **2026-08-11 · T40 done.** Начата Phase 3. Таблица `actions` (миграция 0006) с `reason NOT NULL` — принцип 6 спека закреплён на уровне БД, а не только в генераторе. Роутер `actions.*` и конвертация рекомендации в действие одним кликом; рекомендация валидируется на сервере той же схемой, что и при генерации: клиент может прислать что угодно, а непустой reason — инвариант, а не UI-подсказка (отдельный тест это проверяет). Повторный клик по той же рекомендации дубль не создаёт — совпадение ищется по паре (правило, домен источника): очередь действий рабочий инструмент, а не журнал нажатий. Перевод в done проставляет `completed_at`, возврат из done очищает — без даты завершения эксперимент (T43) не сможет отделить «до» от «после». `origin_rule` сохраняется: видно, из какой рекомендации выросло действие. Verify: 9 юнит-тестов роутера + e2e «рекомендация → действие одним кликом». Решения по ходу: владелец действия не назначается автоматически создателю — тот, кто завёл действие, не обязательно исполнитель; соединение с БД в тестах закрывается на уровне файла, а не первого describe (иначе второй блок падал с CONNECTION_ENDED).
- **2026-08-11 · T34 done. Phase 2 закрыта.** Экран диагностики: донат распределения источников, таблица влиятельных площадок с матрицей присутствия (✓/✗ клиента, чипы конкурентов), вывод и рекомендации с объяснениями. Кнопка «Create action» намеренно неактивна до T40 — очередь действий ещё не построена, и живая кнопка врала бы. Verify: два e2e — на клиенте с покрытием доли складываются в 100%, g2.com классифицирован как площадка отзывов, собственный домен как owned; на клиенте, которого нет в ответах, разрыв «Client in 0 of N» виден и каждая рекомендация несёт объяснение с числами. **Найдено тестом:** первая версия теста ждала рекомендаций у клиента, который упомянут почти везде — их справедливо ноль, и продукт показывает «нечего рекомендовать» вместо выдуманной работы; тест разведён на два случая. Отклонение от плана: sankey заменён донатом и таблицей — на данных одного кластера поток «кластер → типы → источники» вырождается в две колонки и читается хуже таблицы; полноценный граф уместен, когда кластеров много.
- **2026-08-11 · T33 done.** Три правила генерации рекомендаций, каждое с отдельным тестом: (1) влиятельный источник, где есть конкуренты и нет клиента — тип действия выбирается по типу источника (review→review_platform, editorial→pr_editorial, directory→source_outreach, ugc→ugc_community); (2) ни одна собственная страница не цитируется → create_page; (3) собственная страница цитируется, но бренд в этих ответах не звучит → refresh_page, то есть работа над содержанием, а не новый материал. Инвариант 7 сделан типом, а не соглашением: `recommendationSchema` требует непустой `reason`, собрать рекомендацию иначе невозможно, и отдельный тест это фиксирует. Reason всегда содержит числа — долю цитирований, количество и имена конкурентов: агентство перескажет именно его своему клиенту. Каждая рекомендация несёт поле `rule`, чтобы было видно, откуда она взялась. Осознанные ограничения: источник без конкурентов рекомендацию не порождает (отсутствие всех — не разрыв, а нерелевантный источник), на пустых данных список пуст — продукт не выдумывает работу без единого измерения. Verify: 16 юнит-тестов, по одному и более на каждое правило.
- **2026-08-11 · T32 done.** Диагностика: распределение типов источников, влиятельные площадки с матрицей присутствия, вывод «почему клиент проигрывает». Всё — чистые функции над собранными фактами, **без обращения к модели**: вывод должен воспроизводиться из данных, иначе агентство не сможет перепроверить его перед показом клиенту. Verify выполнен буквально по спеку §6.2 — на датасете «конкурент в 16 из 25 источников, клиент в 4» диагностика выдаёт ровно эти числа и вывод о разрыве, создаваемом сторонними источниками. Создан `packages/core/src/copy.ts` (инвариант 2): формулировки выбираются из утверждённого набора, свободный текст запрещён; отдельный тест проверяет отсутствие «proof/proven/guaranteed/caused». Ниже 5 источников вывод не делается вовсе — заключение по трём было бы выдумкой, а его понесут клиенту. Неклассифицированные источники показываются отдельной долей, а не растворяются в «прочем». Присутствие приближается упоминанием в ответе, где источник процитирован, и эта оговорка отдаётся вместе с данными, а не теряется. Verify: 15 юнит + 6 интеграционных тестов. Починено по ходу: интеграционные тесты на общей БД гонялись параллельно и мешали друг другу через глобальную таблицу sources — включена сериализация файлов (`fileParallelism: false`) с объяснением причины.
- **2026-08-11 · T31 done.** Классификатор источников за интерфейсом `SourceClassifier` (CLAUDE.md: все внешние вызовы за абстракцией с mock-реализацией). Двухуровневый кэш: `CachingSourceClassifier` снимает повторы внутри прогона, а таблица `sources` служит постоянным кэшем — уже классифицированный домен не тревожится вовсе. Verify T31 закреплён спаем: второй прогон по тем же данным не делает ни одного вызова классификатора. Порядок жёсткий — словарь важнее модели, известный домен не должен стоить денег; отрицательный ответ тоже кэшируется, иначе неизвестные домены опрашивались бы при каждом прогоне бесконечно. Классификатор без уверенности возвращает null, а не «other». Verify: 14 юнит-тестов + 8 интеграционных. Починено по ходу: `sources` — глобальная таблица и не удаляется вместе с агентством, из-за чего тесты стали зависеть от порядка запуска (тот же класс ошибки, что был с seed) — добавлена явная очистка в beforeEach.
- **2026-08-11 · T30 done.** Начата Phase 2. Таблицы `sources` / `citation_sources` / `source_presence` и rule-классификатор доменов в core (30 тестов): стартовый словарь на 40+ площадок (review, directory, ugc, social, editorial) плюс эвристика документации по поддомену. Ключевые решения: (1) неизвестный домен получает **null, а не "other"** — null означает «нужен следующий классификатор», а тихая запись в other деградировала бы диагностику незаметно; (2) таблица `sources` глобальная, без agency_id — классификация домена не зависит от агентства, и платить модели дважды за один домен незачем; (3) owned определяется доменом клиента и важнее словаря, включая его собственную документацию, иначе своя же страница попадала бы в «gap»; (4) уже классифицированный источник не перезаписывается — правило не должно затирать более точный результат модели или человека. Отдельный тест на `notacmecrm.com`: наивный `endsWith` принял бы его за поддомен клиента. Verify: 30 юнит-тестов словаря и owned-матчинга + 6 интеграционных на пайплайне.
- **2026-08-11 · T25 done. Phase 1 закрыта.** Сквозной e2e золотого пути: регистрация → клиент с алиасами и конкурентами → импорт промптов → расписание → прогон → сырые ответы → цифра на обзоре. Главное утверждение теста не «экраны открываются», а **«цифра сходится»**: тест открывает все 18 сырых ответов, считает, в скольких подсвечен клиент, и сверяет полученную долю с процентом на обзорном экране. Промпта намеренно два — на один клиент упомянут всеми платформами, на другой нет, поэтому доля дробная и совпадение не может быть случайным. Verify: тест зелёный под той же командой, которую запускает CI (`pnpm e2e`); исполнение самого пайплайна CI остаётся заблокированным отсутствием remote (см. T08). Починено по ходу: `count()` в Playwright не ждёт загрузки в отличие от `toHaveCount` — счёт снимался с пустой страницы и тест «проходил» бы на нулях.
- **2026-08-11 · T24 done.** Обзорный экран клиента: четыре stat-карточки (видимость с дельтой к прошлой неделе, разрыв с лучшим конкурентом, плюс честные прочерки для actions и отчётов, которых ещё нет) и линейный график клиент vs конкуренты с фильтрами по платформе и кластеру. Структура приведена к плану §4.1: `/clients/[id]` — обзор, редактирование переехало на `/clients/[id]/settings`. Решения по честности цифры: до первых измерений показывается прочерк, а не ноль (ноль читался бы как «нас не упоминают»); при недоборе сэмплов выводится предупреждение с порогом — цифра посчитана, но подавать её как измерение нельзя (инвариант 6); конкуренты берутся из настроек клиента, а не из данных, чтобы ни разу не упомянутый конкурент был виден нулевой линией, а не исчезал. Verify: 15 e2e зелёные — график строится из visibility_snapshots, цифра переживает reload, фильтр по платформе перезапрашивает данные.
- **2026-08-11 · T23 done.** Экран сырых ответов: полный текст платформы с подсветкой упоминаний (клиент зелёным, конкуренты оранжевым), список процитированных источников, версия модели и стоимость у каждого ответа. Подсветка — чистая функция `highlightMentions` в core (12 тестов), использующая те же правила сопоставления, что и парсер: правило собственных имён и приоритет длинного алиаса. Иначе интерфейс показывал бы одно, а метрика считала другое. Отдельный тест проверяет, что склейка сегментов возвращает исходный текст без потерь — подсветка не имеет права терять символы. Verify: 14 e2e зелёные, подсветка видна на mock-прогоне. **Найдено тестом и исправлено в продукте:** без сохранённого расписания прогон молча измерял только ChatGPT — теперь берётся весь запускной набор платформ. Тихо недомерять и показать это как «видимость» хуже, чем потратить больше: агентство не узнало бы, что охват неполный.
- **2026-08-11 · T22 done.** Панель расписания (каденс, платформы, число сэмплов) и кнопка «Run now» с живым статусом через поллинг. Рефакторинг: измерительный конвейер вынесен из `apps/worker` в общий пакет `@repo/pipeline` — он нужен и воркеру (прогоны по расписанию), и web (ручной запуск), а дублировать оркестрацию нельзя: это единственное место, где решается, сколько вызовов уйдёт к платформам. Ручной запуск в mock-режиме выполняется inline (прогон занимает миллисекунды, воркер поднимать не нужно); в live-режиме он должен уходить в очередь — сотни вызовов не помещаются в один HTTP-запрос, это отмечено комментарием и привязано к T13–T15. Новое расписание получает `next_run_at = now`: иначе агентство создаёт расписание и неделю не понимает, почему данных нет. Прогон без промптов отклоняется с объяснением — пустой прогон стоил бы денег и дал бы пустое окно измерения. Verify: 13 e2e зелёные, «Run now» доходит до `done`, история прогонов переживает reload.
- **2026-08-11 · T21 done.** Экран `/clients/[id]/measure`: CRUD кластеров и промптов, импорт CSV. Свой парсер CSV в core (24 теста): кавычки и запятые внутри значений, CRLF и BOM, колонки в любом порядке, неизвестный интент деградирует в `other`, а не роняет импорт. Ключевое поведение — частично валидный файл импортируется, а проблемные строки называются по номерам: агентство с сотней промптов иначе не поймёт, что чинить. Дубли внутри файла пропускаются, но о них сообщается, иначе счёт импортированных промптов не сошёлся бы с числом строк. Повторный импорт переиспользует одноимённый кластер вместо создания второго. Verify: e2e — CSV на 10 строк даёт 2 кластера и 10 промптов, оба контрольных промпта помечены, данные переживают reload; отдельный тест на битые строки. Починено по ходу: буквальный BOM в регулярке ронял lint (заменён на `\uFEFF`); в e2e слово «control» совпадало и с бейджем, и с чекбоксом — добавлен data-testid.
- **2026-08-11 · T20 done.** Таблица `usage_counters` (агентство × период YYYY-MM в UTC), атомарный инкремент через `ON CONFLICT ... SET x = x + delta` — job'ы платформ пишут в одну строку параллельно, и обычное чтение-запись теряло бы часть расхода. Счётчик увеличивается только после успешной записи ответа: неудавшийся вызов не попадает в счёт агентству. Роутер `billing.usage` отдаёт расход, лимит плана и число активных клиентов; лимиты планов (3/10/25 клиентов, цены 499/1299/2499) вынесены в `PLAN_LIMITS` в core. Превышение allowance — это overage, а не блокировка: остановка измерения посреди недельного окна испортила бы данные сильнее, чем перерасход. Verify: прогон из T17 увеличивает счётчик ровно на 18; 20 параллельных инкрементов дают 20; периоды и агентства не смешиваются. 12 юнит-тестов на биллинговых периодах, включая UTC-границу года.
- **2026-08-11 · T19 done.** Контракт C3: `computeVisibilitySnapshots` — чистая функция, считающая долю ответов с упоминанием клиента за недельное окно (понедельник 00:00 UTC) в четырёх разрезах: кластер×платформа, кластер×все, все×платформа, общий итог. Свёртки считаются по самим ответам, а не усреднением срезов — тест это фиксирует (50% и 100% на двух платформах дают 75%, а не 85%). Доля конкурента считается по числу ответов, а не по числу вхождений в текст, иначе трёхкратное упоминание в одном ответе завышало бы его втрое. Ячейки с недобором сэмплов помечаются `sufficient: false` (порог 3, инвариант 6). Verify: 16 юнит-тестов на математике + 5 интеграционных, включая совпадение с ручным счётом (66.7% на 18 ответах) и стабильность пересчёта. Отклонение от плана: уникальный индекс с `NULLS NOT DISTINCT` drizzle-kit этой версии выразить не умеет, поэтому идемпотентность обеспечивается явным upsert'ом в `upsertVisibilitySnapshot`, а не ограничением БД; причина записана в комментарии к индексу.
- **2026-08-11 · T18 done.** Контракт C2 и парсер: детерминированный matcher по словарю клиента + необязательное уточнение моделью. Ключевые правила, каждое закреплено тестом: (1) правило собственных имён — алиас с заглавной буквы требует заглавной и в тексте, иначе конкурент «Close» находился бы во фразе «close the deal» и раздувал его visibility; (2) длинный алиас важнее короткого, «Acme CRM» не превращается в «Acme»; (3) одна сущность — одно упоминание, сколько бы раз ни повторялась; (4) модель может уточнить тональность, но не может добавить сущность, которой нет в словаре — иначе метрика зависела бы от галлюцинаций парсера; (5) падение модели не роняет разбор. Разбор идемпотентен: переобработка заменяет прежний результат, а не дополняет. Verify: 36 юнит-тестов парсера на всех fixtures (клиент под alias найден, при отсутствии бренда ложных срабатываний нет, citations не теряются) плюс 5 интеграционных на запись в БД. Починено по ходу: тесты воркера не убирали за собой данные, и тест seed, считавший промпты глобально, падал в зависимости от порядка запуска — добавлен afterEach, проверка сужена до кластеров seed.
- **2026-08-11 · T17 done.** Оркестрация прогона: чистая `planRunJobs` (промпт × платформа × сэмпл), `executeRunJob` (вызов адаптера, запись ответа с cost и model_version, сырой текст в storage), `orchestrateRun` со статусами running → done/failed. Отдельная очередь на платформу (`runs-chatgpt` и т.д.) со своим лимитером: общий лимит на одну очередь либо душил бы быструю платформу, либо упирался в 429 у медленной. Частичный успех помечается `failed`, а не `done` — агентство должно видеть, что окно измерения неполное, иначе доля считается по обрезанной выборке. Ключ storage проставляется только после успешной записи файла. Verify: 9 тестов оркестрации (17 у воркера) — 2 промпта × 3 платформы × 3 сэмпла дают ровно 18 ответов и статус done; повторная оркестрация не удваивает данные (ловится уникальным индексом). Починено по ходу: BullMQ 6 запрещает `:` в имени очереди — поймано смоук-запуском процесса, хотя typecheck и lint были зелёные; закреплено тестом.
- **2026-08-11 · T16 done.** Воркер: подключение к Redis, очереди runs/parse/aggregate с retry×3 и экспоненциальным backoff, повторяемый тик планировщика раз в 5 минут, graceful shutdown с закрытием очередей и пула БД. `tickSchedules` находит созревшие расписания, создаёт прогон и сразу сдвигает next_run_at — сдвиг именно в этот момент, иначе следующий тик подхватил бы то же расписание и удвоил расходы на API. Расписание без next_run_at намеренно не подхватывается: иначе первый тик после деплоя запустил бы прогоны по всем клиентам разом. Verify: 8 тестов — созревшее расписание даёт ровно один прогон, повторный тик дубля не создаёт, неактивное и несозревшее игнорируются, плюс реальный round-trip к Redis; отдельно проверен старт процесса (очереди появляются в Redis).
- **2026-08-11 · T12 done.** `MockAdapter` поверх fixtures: сперва точное совпадение промпта (нечувствительное к регистру и пробелам), иначе детерминированный выбор по FNV-1a хэшу — один промпт всегда даёт один ответ, иначе тесты пайплайна стали бы флаки. Реестр `getAdapter(platform, mode)` с точкой расширения `registerLiveAdapter` для T13–T15; `live` без зарегистрированного адаптера даёт понятную ошибку, а не падает в глубине пайплайна. `parseAdaptersMode` по умолчанию возвращает `mock` и падает на мусорном значении: незаданная или опечатанная переменная не должна молча выпускать тесты в сеть и в деньги. Verify: 19 тестов адаптеров (48 в core суммарно), включая проверку, что ответы платформ различаются.
- **2026-08-11 · T11 done.** Контракт C1 зафиксирован в `packages/core/src/adapters/types.ts` (плюс zod-схема для рантайм-проверки — живые API меняют форму ответа без предупреждения). 9 fixture-ответов (по 3 на платформу) на данных seed: клиент AcmeCRM, конкуренты HubSpot/Pipedrive/Close. Покрыты все edge-cases: бренд явно, бренд только под alias, бренда нет вовсе, ответ без citations. Verify: 18 тестов — валидация против AdapterResult плюс проверки самих fixtures (в случае brand-absent ни одного написания бренда, в brand-alias-only нет каноничного, у всех есть model_version и cost_usd по инварианту 6).
- **2026-08-11 · T10 done.** Схема measurement (миграция 0002): prompt_clusters, prompts, run_schedules, runs, responses, mentions, citations + enum'ы платформ/интентов/статусов. Ключевые решения зафиксированы в схеме: `samples_per_prompt` по умолчанию 3 (контракт C3 — visibility по доле, не по одному ответу), уникальный индекс на (run, prompt, platform, sample_index) ловит повторную запись сэмпла как ошибку оркестрации, `raw_storage_key` под сырой ответ для переобработки парсером (инвариант 6), `cost_usd numeric(12,6)` на каждый ответ. Seed расширен: 2 кластера × 5 промптов, по одному контрольному в каждом, расписание на 3 платформы. Verify: 8 тестов, seed идемпотентен при повторном прогоне, типы схемы доступны из входа пакета.
- **2026-08-11 · T08 написан, НЕ отмечен.** `.github/workflows/ci.yml` готов: job `verify` (Postgres+Redis как services, миграции, db:check, lint, typecheck, test, build) и job `e2e` (Playwright chromium, артефакт трейсов при падении). Локально проверено то, что проверяемо: YAML без табов, `pnpm install --frozen-lockfile` проходит, все команды из workflow зелёные. Verify задачи — «pipeline зелёный на PR» — выполнить нельзя: у репозитория нет удалённого origin. Чекбокс остаётся пустым до создания репозитория на GitHub и первого PR (см. блокер в loop/STATE.md).
- **2026-08-11 · T07 done.** Экраны клиентов: список карточками (visibility — заглушка «—» до T24, число конкурентов реальное), создание и редактирование с удалением. Brand names и competitors вводятся списком через запятую. Verify: 8 e2e зелёные — создание клиента с 2 alias и 3 конкурентами, сохранение списков без искажений, переименование; отдельным тестом: клиент другого агентства по прямому URL отдаёт «Client not found» и его имя не утекает в разметку. Починено по ходу: `page.url()` считывался до завершения клиентского перехода и подсовывал тесту URL списка вместо карточки — тест «проходил» бы, проверяя не то; новые маршруты требуют чистой пересборки для регенерации типов `typedRoutes`.
- **2026-08-11 · T06 done.** Settings: имя агентства, brand colour, загрузка логотипа, список команды и приглашения. Хранилище за интерфейсом StorageAdapter в core (11 юнит-тестов на валидацию и изоляцию ключей по agency), реализация — локальный диск в `.storage/`, отдача через `/api/files/[...key]`; S3 подключается той же абстракцией. Verify: e2e — логотип и цвет переживают reload, файл реально отдаётся с `content-type: image/png`, переименование видно в сайдбаре. Починено по ходу: `getByRole("alert")` конфликтует со служебным route-announcer'ом Next (перешли на data-testid); Better Auth в production-сборке включает rate limiting и роняет серию регистраций в e2e — отключается только флагом `DISABLE_RATE_LIMIT` для прогона, в проде остаётся.
- **2026-08-11 · T05 done.** Route group (app) с общим каркасом: сессия проверяется один раз в layout, сайдбар (Overview/Clients/Settings) с подсветкой активного пункта, топбар, имя агентства и почта пользователя. Компоненты PageHeader и EmptyState — empty state теперь обязательный примитив, а не разметка на каждой странице. Verify: 4 e2e зелёные, включая прогон всех защищённых роутов с проверкой пустой консоли и редиректа анонима на /login. Починено по ходу: после переноса страницы в route group .next/types держал ссылки на удалённый маршрут и ронял typecheck — нужна чистая пересборка; тест T03 подтянут под новую структуру (имя агентства переехало в сайдбар).
- **2026-08-11 · T04 done.** tRPC 11 + superjson: `protectedProcedure` (требует сессию И agencyId), `roleProcedure(min)` с рангами member<admin<owner, `assertTenant` (чужой ресурс → NOT_FOUND). Роутеры `agency` (get/update/members/invites/invite/inviteInfo) и `clients` (list/get/create/update/delete) с enforcement лимита тарифа на создание клиента. Приглашения: токен на 7 дней, ссылка пишется в лог (транспорта писем в MVP нет), регистрация по приглашению присоединяет к существующему агентству вместо создания нового; страница `/invite/[token]` с заблокированным полем почты. Verify: 10 юнит-тестов, в том числе «несуществующий и чужой клиент неразличимы» (сравнение кодов ответа, а не только проверка NOT_FOUND). Починено по ходу: `vite-tsconfig-paths` ESM-only и не грузится загрузчиком конфига vitest 2 — алиас задан вручную; zod выровнен до v4 (требование better-auth).
- **2026-08-11 · T03 done.** Better Auth (email+password) поверх нашей таблицы `users` — agency_id и role живут на том же ряду, что и учётка; добавлены `sessions`/`accounts`/`verifications`/`invitations` (миграция 0001). Регистрация создаёт агентство в `databaseHooks.user.create.before` и делает пользователя owner'ом; id генерирует Postgres (`generateId: false`). Страницы `/login`, `/signup`, защищённый `/dashboard` с empty state. Verify: Playwright 2/2 — signup→logout→login и отказ при неверном пароле. Решение в рамках MVP: имя агентства выводится из домена почты (`deriveAgencyName`), переименование — в T06 Settings; поля `agencyId`/`role` объявлены `input: false`, чтобы клиент не мог подставить чужой agency при регистрации. Починено по ходу: drizzle-адаптер ищет таблицы по `modelName`, а не по дефолтным именам моделей Better Auth; `drizzle-orm` больше не импортируется из `apps/web` — запросы вынесены в `@repo/db/queries`.
- **2026-08-11 · T02 done.** Схема tenancy (agencies/users/clients + enum'ы plan/user_role/client_status, индексы по agency_id, brand_names/competitor_names как text[]), миграция `0000_square_professor_monster.sql`, идемпотентный seed (Demo Agency, owner, AcmeCRM + Northwind Analytics) и 4 интеграционных теста на его содержимое. Verify: `pnpm db:seed` дважды подряд даёт тот же результат; `pnpm --filter @repo/db test` — 4/4 зелёные. Починено по ходу: drizzle-kit собирает конфиг и схему как CJS и не резолвит ESM-расширения `.js` в импортах — в `packages/db` перешли на импорты без расширений (bundler-резолюция TS их не требует).
- **2026-08-11 · T01 done.** docker-compose (Postgres 16 на 5433, Redis 7 на 6380 — нестандартные порты, чтобы не конфликтовать с локальными установками), Drizzle + postgres.js, `.env.example`, программный мигратор (переживает пустую папку миграций). Verify: `docker compose up -d && pnpm db:migrate && pnpm db:check` зелёные, плюс typecheck/lint/test/build. Уточнение плана: verify T01 усилен `db:check`, т.к. `db:migrate` при пустой схеме не открывает соединение. Починено по ходу: `dotenv` не находил корневой `.env` при запуске из папки пакета (теперь поиск вверх по дереву); `next-env.d.ts` ронял eslint (в ignore + gitignore).
- **2026-08-11 · T00 done.** Монорепо: pnpm workspaces + Turborepo 2, apps/web (Next 15 + React 19 + Tailwind v4 + shadcn-совместимые токены), apps/worker (tsx-скелет), packages/core, packages/db. Общие tsconfig.base / eslint flat config / prettier. Verify: `pnpm install && pnpm build && pnpm lint` зелёные (плюс typecheck и test). Отклонение от плана: `next/font/google` убран — в окружении TLS-перехват, шрифт грузился по сети на этапе билда и ронял сборку; вместо него font-stack, self-hosted Inter возможен в T05.
