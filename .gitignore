node_modules/
.next/
dist/
.turbo/
coverage/
*.tsbuildinfo
next-env.d.ts

# Игнорируется всё семейство, а примеры — по исключению. Перечислять файлы
# поимённо значит однажды завести .env.staging и закоммитить его секреты.
.env*
!.env.example
!.env.production.example

loop/run.log

# Внутренние рабочие файлы: остаются локально, в публичный репозиторий
# не входят. Спеки — потому что там позиционирование, тарифы, unit-экономика
# и план выхода на рынок; остальное — наша кухня, а не документация продукта.
docs/
loop/
TASKS.md
IMPLEMENTATION_PLAN.md
CLAUDE.md

.DS_Store

test-results/
playwright-report/
.storage/
